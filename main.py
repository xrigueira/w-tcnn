# TODO: load the predictions from the transformer and perform inference with the CNN model

# Load the U-Net model
# model = torch.load("results/models/unet_model.pth").to(device)
# print('Loaded PyTorch model from results/models/unet_model.pth')